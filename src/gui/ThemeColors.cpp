// ThemeColors.cpp — ensure moc processes the ThemeManager Q_OBJECT.
// The ThemeManager implementation is inline in ThemeColors.h; this file
// only exists so AUTOMOC picks up the Q_OBJECT macro.
#include "ThemeColors.h"
